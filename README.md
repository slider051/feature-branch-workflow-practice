# feature-branch-workflow-practice

- GitHub에서 레포 생성

- Clone

git clone <레포주소>

- 레포 폴더로 이동

cd 레포이름

- 브랜치 생성

git switch -c 브랜치명

- 파일 작업

커밋

git add .
git commit -m "첫 커밋"

푸시

git push origin 브랜치명
